# Final release evidence

The evidence attachment for the definitive full release-signoff audit is:

`planar-strict-convex-hyperrigidity-final-evidence-v1.zip`

SHA-256:

```text
02e04371149660db9a627eafb100ce9c09d703242f7a6d0d901ea32a1b37b141
```

Size: 1,137,773 bytes.

The archive contains the exact corrected TeX, all frozen source artifacts,
all eight prior audit returns, both final literature reports, the Aristotle
archive and extracted project, a declaration scan, the fresh GitHub Actions
log and artifact, the extracted eight-page PDF, build scripts and workflow,
the documentary-only TeX diff, and a root checksum manifest covering all 55
payload files.

Archive integrity was tested with `unzip -t`. From the extracted
`final_release_evidence_v1` directory, verify every included byte sequence
with:

```bash
shasum -a 256 -c ROOT_SHA256SUMS
```

This evidence package documents an ordinary AI-generated and AI-audited
mathematical proof. It does not claim human peer review, Lean verification of
the headline theorem, absolute historical priority, or the unrestricted
affine-dimension-three result.
