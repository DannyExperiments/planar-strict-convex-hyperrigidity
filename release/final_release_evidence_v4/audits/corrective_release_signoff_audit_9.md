## Release-signoff matrix

|  # | Category                                    |  Verdict |                                         Confidence                                        |
| -: | ------------------------------------------- | :------: | :---------------------------------------------------------------------------------------: |
|  1 | Mathematics                                 | **PASS** |                                            High                                           |
|  2 | Literature and scope                        | **PASS** | High for cited-result scope; moderate-to-high for the expressly qualified negative search |
|  3 | Provenance, authorship, and disclosure      | **FAIL** |                                            High                                           |
|  4 | Reproducibility and bundle self-containment | **FAIL** |                                            High                                           |
|  5 | Formalization boundary                      | **PASS** |                                            High                                           |
|  6 | Release readiness and public-claim scope    | **PASS** |                                            High                                           |

The externally attached TeX and the copy in the ZIP are byte-identical, with SHA-256:

```text
1902dc564a0a2fc09cd6a04fa6d56943deae15453b79d9841a3998b462426014
```

## 1. Mathematics — PASS

I independently checked all fourteen numbered theorem, lemma, and corollary statements, the numbered remark, their proofs, hypotheses, endpoint cases, measurability claims, operator arguments, and stated consequences.

| TeX location         | Item                              | Independent result |
| -------------------- | --------------------------------- | :----------------: |
| 69–80; proof 566–624 | Main planar POVM/PVM theorem      |      **PASS**      |
| 90–102               | Square compression corollary      |      **PASS**      |
| 172–204              | Compact-fiber selection           |      **PASS**      |
| 206–230              | Borel subgradient selector        |      **PASS**      |
| 244–300              | Critical reciprocal energy        |      **PASS**      |
| 306–354              | Common-domain localization        |      **PASS**      |
| 356–371              | Affine exposure                   |      **PASS**      |
| 373–410              | Interval reconstruction           |      **PASS**      |
| 417–444              | Planar boundary stratification    |      **PASS**      |
| 448–483              | Measurable extreme representation |      **PASS**      |
| 519–525              | Scherer theorem in POVM/PVM form  |      **PASS**      |
| 527–562              | Extreme-supported reconstruction  |      **PASS**      |
| 626–630              | Dimension limitation remark       |      **PASS**      |
| 634–675              | Tuple compression corollary       |      **PASS**      |
| 679–696              | Hyperrigidity corollary           |      **PASS**      |

The decisive checks were:

* **Endpoint cases.** The affine-dimension-zero case in the energy lemma is handled explicitly at lines 253–257. The singleton interval is handled explicitly at lines 379–382. No illicit use of an interior argument remains in either case.
* **Compact-fiber selection.** Fiber-distance functions are lower semicontinuous; the first-index selections are Borel; the successive selections satisfy a summable Cauchy estimate; and the pointwise limit lies in the closed fiber.
* **Subgradient measurability.** Compact containment in (\operatorname{ri}K) supplies a uniform interior ball and hence a uniform subgradient bound. The full graph is compact, allowing the preceding selection lemma to be applied.
* **Uniform Bregman separation.** Although the selected subgradient need not be continuous, the minimum is taken over the compact full subgradient graph. Strict convexity excludes zero defect off the diagonal, so the asserted uniform (c>0) is valid.
* **Critical-energy argument.** The monotonicity identity
  [
  D_z(t)+D_t(z)=\langle p(t)-p(z),t-z\rangle
  ]
  is correct. The Minty variables (U=z+p(z)), (W=z-p(z)) give injectivity of (U) and the required Lipschitz estimate. The grid pullback gives pair mass bounded below by a constant times (r^d), and layer-cake integration produces (\int_0 r^{d-3},dr=\infty) exactly for (d=1,2).
* **Douglas factorization.** From (cT\le M_z), the orientation
  [
  \sqrt c,T^{1/2}=M_z^{1/2}C_z
  ]
  is the correct Douglas orientation. The proof does not require a measurable choice of (C_z): it obtains a pointwise uniform bound, then integrates the separately established jointly Borel reciprocal-Bregman kernel.
* **Functional calculus.** The identity at lines 342–344 follows from the spectral calculus for (M_z), the fixed scalar spectral measure generated by (\eta=T^{1/2}u), and the bound (\lambda h_n(\lambda)\le1).
* **Exposure and interval reconstruction.** The indicator inequality (1_{{r\ge1/n}}\le nr) is correctly converted into an operator inequality. Endpoint support, interior localization in both orders, regularity, polarization, and the positivity implications (RTR=R\Rightarrow TR=R) and (RTR=0\Rightarrow TR=0) correctly reconstruct the interval measure.
* **Planar stratification.** In dimension two, every nonextreme boundary point lies in the relative interior of a maximal exposed segment. These interiors are pairwise disjoint and relatively open, hence at most countable. Their endpoints are extreme, and their union has relatively open complement, so (\operatorname{ex}K) is compact.
* **Extreme-point kernel.** The barycenter relation is a closed relation with nonempty compact fibers. The selected representing measure at an extreme point must be the corresponding point mass; the nontrivial-decomposition argument is valid.
* **Boundaryization.** Evaluation (\nu\mapsto\nu(S)) is Borel for Borel (S); scalar monotone convergence gives countable additivity; diagonal scalarizations followed by polarization justify the bounded-kernel Fubini identity.
* **Strict Jensen step.** A selected representing measure is a point mass exactly on (Z=\operatorname{ex}K). For any non-point-mass measure, the independent-copy argument gives strict Jensen inequality. Positivity therefore forces the original POVM to be supported on (Z).
* **Main reconstruction.** Interior, flat-face, and extreme-point diagonal blocks are each reconstructed. The final positive-contraction argument annihilates every cross term across the countable spectral partition.
* **Tuple consequence.** Affine separation correctly places the compressed joint spectrum in (K). Equality of the POVM and PVM gives equality of second moments, and the positive leakage identity
  [
  V^*T_j^2V-A_j^2
  =((I-P)T_jV)^*((I-P)T_jV)
  ]
  forces reduction.
* **Hyperrigidity consequence.** The POVM/UCP correspondence is used with the correct orientation, the affine functions generate (C(K)), and the stated separability condition concerns (C(K)), not the acting Hilbert space.

I found no false mathematical conclusion, missing hypothesis, unhandled endpoint, invalid measurability assertion, reversed operator inequality, or unsupported consequence.

## 2. Literature and scope — PASS

The cited-result descriptions and scope boundaries are accurate.

* Brown’s result is genuinely one-variable: weak convergence of self-adjoint operators together with weak convergence after one continuous strictly convex function forces strong convergence, with the corresponding compression-invariance consequence. Scherer’s planar theorem concerns (A(K)) for compact convex (K\subset\mathbb R^2), exactly as used for the extreme-boundary reconstruction. ([[arXiv](https://arxiv.org/abs/2411.11709?utm_source=chatgpt.com)][1])
* Scherer’s later theorem is specifically for compact spectrahedra with closed extreme boundary. Shankar’s stated normal-operator result gives hyperrigidity of ({T,TT^*}), and Pietrzycki–Stochel study explicitly specified monomial systems in a single commutative generator. Davidson–Kennedy give the cited Choquet-order/unique-extension relationship rather than a blanket solution of the commutative conjecture. ([[arXiv](https://arxiv.org/abs/2601.16075)][2])

The graph-hull distinction at TeX lines 130–159 is also correct:

[
\dim\operatorname{aff}L_g
=\dim\operatorname{aff}K+1
]

when (g) is nonaffine. Thus a one-dimensional (K) produces a planar graph hull covered by Scherer, while a genuinely planar (K) produces a three-dimensional graph hull not covered by his planar theorem.

The current language is properly qualified:

* TeX 148–151: “located no prior theorem with that full scope; this negative search is not a claim of historical priority.”
* TeX 726–729: “this remains evidence from a negative search, not a claim of historical priority.”
* `literature/README.md:18-19`: “apparently new in affine dimension two” followed by an explicit denial that this proves absolute priority.
* `literature/LITERATURE_VERDICT.md:27-30`: “no prior theorem located,” “apparently new,” and “absolute historical priority: not established.”

No absolute “first proof,” “first result,” or equivalent historical-priority claim appears in the current TeX or current controlling metadata.

## 3. Provenance, authorship, and disclosure — FAIL

Authorship and disclosure themselves pass:

* TeX 26 identifies `DannyExperiments` as author.
* TeX 27–30 attributes generation of the ordinary proof to GPT-5.6 Pro, manuscript/audit/provenance work to Codex, and curation to DannyExperiments.
* `AI_DISCLOSURE.md:3-13` uses the same division of roles.
* The audit count, AI-versus-human-review distinction, Lean limitations, toolchain version, and literature-search qualification are internally consistent.

The category nevertheless fails because one current repository-relative provenance path is wrong.

`PROVENANCE.md:5-8` establishes the controlling convention:

> “Unless a path is explicitly labeled bundle-local, every path in this file is relative to the repository root. The corrective evidence bundle mirrors these repository paths from its own top-level directory.”

But `PROVENANCE.md:119-121` says:

> “This build predates the final disclosure, citation, and release-wording
> repairs requested by `repaired_manuscript_hostile_audit.md`…”

Under the declared convention, that denotes:

```text
<repository-root>/repaired_manuscript_hostile_audit.md
```

No such file exists. The actual mirrored repository-relative path is:

```text
audits/repaired_manuscript_hostile_audit.md
```

That correct path is itself used at `PROVENANCE.md:29`.

This is an internally inconsistent current provenance reference. It is not cured by the file’s presence under `audits/`, because the explicit path convention makes the bare root-relative pathname authoritative.

I found no other unintended unresolved path in `PROVENANCE.md`. The absent enclosing `release/` directory is expressly exempted at `PROVENANCE.md:7-8` and therefore is not a defect.

## 4. Reproducibility and bundle self-containment — FAIL

### Required root command

From the extracted ZIP root, I ran exactly:

```bash
bash VERIFY_BUNDLE.sh
```

Observed result:

```text
exit status: 1
stdout: empty
stderr: empty
```

The cause is mechanical and reproducible.

At `VERIFY_BUNDLE.sh:4`:

```bash
script_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
```

The script is located at the extracted bundle root, so this computes the **parent of the bundle**, not the bundle root.

The no-argument branch at `VERIFY_BUNDLE.sh:54-55` then executes:

```bash
if [[ "$#" -eq 0 ]]; then
  verify_directory "$script_root"
```

The first test inside `verify_directory`, at line 10, searches the parent directory for `ROOT_SHA256SUMS` and exits unsuccessfully.

The defect was introduced by `scripts/build_release_bundle.sh:47`:

```bash
cp scripts/verify_release_bundle.sh "$bundle_root/VERIFY_BUNDLE.sh"
```

The original script’s `../` calculation is correct while the script remains under `scripts/`; copying it to the bundle root without changing that calculation breaks its no-argument entry point.

This contradicts the exact release instruction at `BUNDLE_MANIFEST.md:41-50`:

> “From the extracted `final_release_evidence_v2` directory:
>
> `bash VERIFY_BUNDLE.sh`
>
> The script must finish with:
>
> `release-bundle: PASS`”

That documented command does not finish with the stated result.

For diagnostic separation:

```bash
bash VERIFY_BUNDLE.sh .
```

does pass, as does:

```bash
bash scripts/verify_release_bundle.sh
```

Both finish with:

```text
release-bundle: PASS
```

Those alternative invocations show that the underlying verification logic and payload are otherwise sound, but they do not make the explicitly required and documented no-argument root command reproducible.

### Independent bundle checks that passed

* Outer ZIP compressed-data integrity: **PASS**.
* ZIP layout: exactly one top-level extracted directory.
* `ROOT_SHA256SUMS`: **71 listed payload files and exactly 71 actual payload files**, excluding the checksum file itself; all 71 hashes verify.
* `SOURCE_MANIFEST.sha256`: **17 of 17 entries present and hash-correct**.
* Attached TeX and bundled TeX: byte-identical.
* Both source artifacts: present.
* All manifest-listed audits: present.
* Both preliminary searches: present.
* Both final literature reports: present.
* CI job log: present and hash-correct.
* Workflow and scripts: present.
* CI artifact ZIP: valid and contains exactly one PDF.
* Extracted bundled PDF: present.
* Documentary-only TeX patch: present; it applies to the preserved audited TeX and reproduces the current TeX exactly.
* Artifact PDF and bundled PDF: byte-identical, both with SHA-256:

  ```text
  27ffab5e1a5d3ab99f2bbe4f5b44d6c62b4d7d79aa8860627e1a73f5e2edadb1
  ```
* Bundled PDF: eight A4 pages and unencrypted.
* No legacy `sources/` directory.
* No legacy `audit_inputs/` directory.

Thus the payload-coverage and self-containment checks pass, but the category fails because the mandatory root verification command is broken and its current manifest claim is unreproducible as written.

## 5. Formalization boundary — PASS

The immutable archive and documentary-corrected tree satisfy all six requested conditions.

### Foundational escape scan

After stripping Lean comments, the foundational files:

```text
RequestProject/POVM.lean
RequestProject/Main.lean
```

contain no occurrence of:

```text
sorry
admit
sorryAx
unsafe
new axiom declaration
new constant declaration
@[implemented_by]
```

The supporting metadata’s narrower phrase “no disclosed proof escape” is therefore accurate.

### Exactly three standalone `sorry` proofs

The corrected `RequestProject/Statements.lean` contains exactly three standalone proof holes:

* line 65 — `thm_main`
* line 128 — `cor_tuple`
* line 160 — `cor_square`

There are no additional Lean proof escapes in the project’s substantive Lean files.

### Original versus corrected code

The original Aristotle archive has SHA-256:

```text
1887d86be11c597cfcbc604346b3efb55260cb3bbecbf69720443c5210b6bc1f
```

Independent comparison confirms:

* `POVM.lean` and `Main.lean` are byte-identical.
* `lean-toolchain`, `lakefile.toml`, `lake-manifest.json`, the archive README, and `.gitkeep` are byte-identical.
* `Statements.lean` differs only in comments.
* After stripping Lean comments, the code in every Lean source is unchanged.
* The remaining differences are documentary corrections in `ARISTOTLE_SUMMARY.md` and `GAP_REPORT.md`.

### Exact statement boundary

`Statements.lean:56-65` gives `thm_main` with:

* finite-dimensional real ambient space;
* compact convex (K);
* `finrank (vectorSpan ℝ K) ≤ 2`;
* arbitrary complete complex Hilbert space;
* normalized POVM and PVM;
* continuity and strict convexity on (K);
* equality on every affine function and on (g);
* conclusion `E.Agree F.toPOVM`.

This matches the manuscript’s main POVM/PVM theorem.

By contrast, `cor_tuple` and `cor_square` place:

* (A_j=PT_jP) on the full ambient Hilbert space;
* a unital `JointCFC` for that full-space tuple;
* the functional-calculus equality on the full ambient space.

They are therefore stronger ambient-space surrogates, not exact formulations of the manuscript’s compressed-space statements on (H=\operatorname{ran}P). This is stated correctly at:

* `Statements.lean:69-77`
* `Statements.lean:101-111`
* `Statements.lean:134-144`
* `formalization/ARISTOTLE_SCOPE_CORRECTION.md:34-59`
* TeX 711–721.

No current headline Lean-verification claim is made. The absence of completed proofs and independent replay is disclosed rather than obscured.

## 6. Release readiness and public-claim scope — PASS

The public mathematical scope is correctly limited.

* TeX 66–79 presents the result as an ordinary proposed proof for affine dimension at most two.
* TeX 626–630 expressly states that the argument does **not** assert the unrestricted affine-dimension-three theorem.
* TeX 700–709 identifies the audits as AI-assisted and explicitly says they are not human peer review.
* TeX 711–721 states that all three headline Lean declarations remain `sorry` and that no headline result is formally verified.
* TeX 726–729 qualifies the literature conclusion as negative-search evidence rather than historical priority.
* `README.md:25-31`, `AI_DISCLOSURE.md:27-29,46-53`, and `CLAIMS_MATRIX.md:14-18,20-29` preserve the same limitations.

There is no unrestricted dimension-three claim, human-peer-review claim, full Lean-verification claim, or absolute historical-priority claim.

**OVERALL FAIL — Categories 3 and 4 fail because `PROVENANCE.md:119-121` contains the unresolved repository-relative path `repaired_manuscript_hostile_audit.md` instead of `audits/repaired_manuscript_hostile_audit.md`, and because the documented extracted-root command `bash VERIFY_BUNDLE.sh` exits with status 1 owing to the incorrect parent-directory computation at `VERIFY_BUNDLE.sh:4`; every mathematical, literature-scope, formalization-boundary, payload-integrity, and public-claim check otherwise passes.**

[1]: https://arxiv.org/abs/2411.11709?utm_source=chatgpt.com "[2411.11709] The Hyperrigidity Conjecture for compact convex sets in $\mathbb{R}^2$"
[2]: https://arxiv.org/abs/2601.16075 "The Hyperrigidity Conjecture for Spectrahedra"
